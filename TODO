default output name to device+subnet
	to work with multiple instances
	use domain socket?

packaging
	daemonizing
		figure out dealing with multiple instances
			and different options for each
		procd for openwrt
			started
		systemd for everything else?
			first debian package attempt done (thanks TQ)
	find a place for macs.csv

get_if_mac()
	make portable

CLAR_COUNT.SH:

	make sure log() doesn't lose data
