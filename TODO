confirm ARP packets' frames leave with a CRC

implement nag()'s IPv6 support
	NDP ICMPv6 Neighbor Solicitation (type 135) packet?

default output name to device+subnet
	to work with multiple instances
	use domain socket?

packaging
	daemonizing
		figure out dealing with multiple instances
			and different options for each
		procd for openwrt
			started
		systemd for everything else?
			first debian package attempt done (thanks TQ)
	find a place for macs.csv

add long options

get_if_ip()
	allot of addresses
		selecting fe80/16
			save all, nag with one with the same prefix?

CLAR_COUNT.SH:

	make sure log() doesn't lose data
